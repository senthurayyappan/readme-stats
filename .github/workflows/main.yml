name: Run Hello World Action
on: [push]  # This will run the action on every push

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run JS Action
        uses: ./
        with:
          who-to-greet: 'Senthur Ayyappan'